[project]
name = "ashmatics-llm-llama"
version = "0.2.0"
description = "FastAPI wrapper for llama.cpp"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.100.0",
    "uvicorn[standard]>=0.20.0",
    "pydantic>=2.0.0",
    "httpx>=0.24.0",
    "huggingface_hub>=0.20.0",
    "mcp>=1.0.0",
    "openai>=1.0.0",
]

[tool.uv]
dev-dependencies = [
    "pytest",
]
